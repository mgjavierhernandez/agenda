# Variables para Docker Compose (infra/docker/docker-compose.yml).
# Copiar al archivo .env de la raiz del repositorio.
# Valores SOLO PARA DESARROLLO LOCAL.

POSTGRES_USER=agenda
POSTGRES_PASSWORD=agenda_dev_password
POSTGRES_DB=agenda_dev
POSTGRES_PORT=5432