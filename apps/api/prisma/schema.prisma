generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Esta etapa NO define modelos de negocio todavia.
// Los modelos (User, Role, Tenant, Task, etc.) se anadiran
// en la siguiente fase, siguiendo docs/00-analisis-y-arquitectura.md.